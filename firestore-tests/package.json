{
  "name": "swuper-defense-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project demo-swuper-defense --only firestore \"node --test firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
